name: Tests
on: push
jobs:
  tests:
    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
      - name: Install just and cargo-make
        uses: taiki-e/install-action@v2
        with:
          tool: just,cargo-make
      - name: Install cargo-near
        run: curl --proto '=https' --tlsv1.2 -LsSf https://github.com/near/cargo-near/releases/latest/download/cargo-near-installer.sh | sh
      - name: Build contracts
        run: just build
      - name: Run tests
        # --locked keeps the MSRV pins (time/deflate64/tempfile/schemars) and near-workspaces
        # 0.21.0 intact; a stray `cargo update` would break the build on the pinned 1.86 toolchain.
        run: cargo test --locked
